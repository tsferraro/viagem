# Backups locais (gerados pelo deploy.sh) ficam fora do repo
/.skill-backups/

# Builds temporários
/tmp-build/
*.tmp.html

# data.json de drafts (não publicáveis)
data-draft*.json
.draft-*.json

# Entregáveis Word gerados pela skill destination-scout (regeneráveis · não versionar)
*.docx

# Editor cruft
.DS_Store
.idea/
.vscode/
*.swp
